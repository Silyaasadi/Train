import 'package:flutter/material.dart';

class LocationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Page de localisation", style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),
    );
  }
}
